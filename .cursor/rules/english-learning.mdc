---
description: 打开项目后：先翻译单词/句子，再入库与其余解析
alwaysApply: true
---

# English Learning — 默认行为

本仓库是英语学习项目。用户打开项目后**直接输入单词或句子**，即按下面执行，无需再确认格式。

权威说明：`vocabulary/METHOD.md`  
模板：`templates/word-entry.md`、`templates/sentence-entry.md`

---

## 总流程（强制：先翻译，再入库）

接到单词或句子时：

1. **先翻译** — 对话回复**开头**立刻给出中文意思  
   - 单词：1–3 个高频中文义  
   - 句子/段落：一句英 + 一句中，覆盖全部原文  
2. **再入库与其余操作** — 写 `entries/`、更新 `words.md`、追加 `log/`、补英文解释 / 原子意思 / 词根 / 短语 / 例句 / 语法等  

禁止：先做一堆文件写入、回复末尾才给翻译。用户应先看到翻译。

---

## 输入 = 单词（默认）

识别：单个词、几个词、或「xxx 什么意思」——按单词处理。

顺序：

1. **对话先给翻译**（高频常用意思：中文）  
2. **紧接着给英文解释**（每个义项用英语说清怎么理解）  
3. **再给原子意思**（一个核心图景；英文取词 vs 中文取词）  
4. 写入 `vocabulary/entries/<word>.md`（文件名小写，空格改 `-`）  
5. 更新 `vocabulary/words.md` 索引一行  
6. 追加 `vocabulary/log/YYYY-MM-DD.md`  
7. 在对话继续给出词根、短语、常用句子（与条目同一结构）

条目必须含五块（缺一不可；**先翻译，再英文解释，再原子意思，再归类**）：

1. 高频常用意思（1–3）— 每条：**中文 + 英文解释**  
2. 原子意思 — 一个核心图景；说明为何看起来像一词多义  
3. 词根 — 构词 / 真同源 / 形近勿混  
4. 常用短语  
5. 常用句子（每句：逐词翻译 + 语法拆解）

原子意思写法：英文描述「动起来/性状什么样」，中文描述「看起来是哪一类」；同一原子套不同对象，中文才拆成不同词。只写真词源，假亲戚单独标明。示范见 `vocabulary/entries/plane.md`。

来源默认 `chat`；从 inbox/线上粘贴则用 `online`；掌握默认 `new`。

---

## 输入 = 句子 / 段落

识别：完整英文句，或一段/多段英文（含列表、证据来源等）。

### 对话展示（必须先做）

**默认用「一句英文 + 一句中文」交替**，覆盖用户贴出的全部句子：

```
English sentence one.
中文翻译一句。

English sentence two.
中文翻译一句。
```

规则：
- 按原文句号/问号/感叹号断句；列表、小标题也可各算一行，同样英→中
- **先完整输出双语对照**，再做入库与补充
- **单句**同样先英后中；若用户还要逐词翻译/语法拆解，再追加
- 不要只给整段大意、漏掉逐句对照

### 翻译之后再做

1. 可选写入 `vocabulary/sentences/YYYY-MM-DD-<短标题>.md`（存档也保留双语对照）  
2. **只把较复杂的词**按单词流程写入 `entries/` + `words.md`  
3. **禁止**把句中每个词都建条目；跳过冠词、be、简单代词和极基础词  
4. 追加 `vocabulary/log/YYYY-MM-DD.md`  
5. 需要时再补：难词、关键搭配、简短语法点

在回复末尾列出「已入库难词：…」。

---

## 每日 AI 新闻

用户说「生成今天的 AI 新闻」等：

1. 读 `words.md` / `entries/`  
2. 用已学词改写 1 则 AI 新闻 → `news/YYYY-MM-DD.md`  
3. 必须新词才入库，来源 `news`

---

## 原则

- 用户丢词 = **先翻译**，再解析并写入 vocabulary，不要只口头讲一遍就结束  
- `words.md` 是索引；完整内容在 `entries/`  
- 不擅自 git commit / push  
- 回复清晰、中文为主解释，英文例句保留原文  

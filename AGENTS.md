# Agent 说明（打开本项目即读）

本仓库是英语学习项目。用户直接输入英文单词或句子时，按本文件 + `vocabulary/METHOD.md` 执行，不必再确认格式。

**总顺序：先翻译 → 再入库与其余解析。禁止先写一堆文件、回复末尾才给翻译。**

细则：`.cursor/rules/english-learning.mdc`  
模板：`templates/word-entry.md`、`templates/sentence-entry.md`  
示范：`vocabulary/entries/plane.md`

---

## 单词（默认）

识别：单个词、几个词、或「xxx 什么意思」。

**对话回复必须按此顺序（缺一不可）：**

1. **开头立刻中文义**（1–3 个高频意思）
2. **英文解释**（每个义项用英语说清怎么理解，不要只丢同义词）
3. **原子意思**（一个核心图景：英文取「动起来 / 性状」，中文取「看起来是哪一类」；同一原子套不同对象，中文才拆成不同词）
4. 再给 **词根**、**常用短语**、**常用句子**（每句：逐词翻译 + 语法拆解）

同时写入：

- `vocabulary/entries/<word>.md`（文件名小写，空格改 `-`）
- `vocabulary/words.md` 索引一行
- `vocabulary/log/YYYY-MM-DD.md`

条目内五块顺序与上面相同：高频常用意思（中文 + 英文解释）→ 原子意思 → 词根 → 短语 → 句子。只写真词源；形近假亲戚单独标明。

来源默认 `chat`；掌握默认 `new`。

---

## 句子 / 段落

识别：完整英文句，或一段/多段英文。

**先完整「一句英 + 一句中」对照**，覆盖用户贴出的全部原文；再挑较难的词按单词流程入库。

- 不要只给整段大意
- **禁止**把句中每个词都建条目；跳过冠词、be、简单代词和极基础词
- 需要时再补搭配 / 语法；末尾列出「已入库难词：…」

---

## 新闻

用户说「生成今天的 AI 新闻」时：用已学词改写 1 则 → `news/YYYY-MM-DD.md`；必须新词才入库，来源 `news`。

---

## 原则

- `words.md` 是索引；完整内容在 `entries/`
- 不擅自 git commit / push
- 回复清晰、中文为主解释，英文例句保留原文
